{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "salmon",
            "url": "http://localhost:4001",
            "webRoot": "${workspaceFolder}/src"
        }
    ]
}